# Plan 03-04: Node Persistence

**Wave:** 3
**Depends on:** 03-02-PLAN.md (Error Display) AND 03-03-PLAN.md (Canvas Search)
**Files modified:**
- src/components/canvas/NodeCanvas.tsx
- src/services/node-persistence.ts
- src/store/app-store.ts
- src/types/index.ts

**Requirements:** CANV-05, FILE-07, VALD-08

## Pre-requisites
- [ ] 03-02 (Error Display) completed and tested
- [ ] 03-03 (Canvas Search) completed and tested

## Tasks

<task id="03-04-01">
<read_first>
- src/types/index.ts
- .planning/phases/03-validation-search/03-RESEARCH.md (sidecar file format)
</read_first>

<acceptance_criteria>
- NodePersistence schema defined with all required properties
- Includes positions, collapsed state, and view settings as outlined in research
- Type-safe and compatible with existing store types
</acceptance_criteria>

<action>
Add NodePersistence types to src/types/index.ts:
1. Define JsonEngineState interface matching the format from research:
   - version: "string"
   - lastModified: Date or ISO string
   - positions: Record<string, {x: number, y: number}>
   - collapsed: Record<string, boolean>
   - view: {zoom: number, position: {x: number, y: number}}

2. Add supporting types for position management
</action>
</task>

<task id="03-04-02">
<read_first>
- src/lib/json-to-graph.ts (current node creation logic)
- .planning/phases/03-validation-search/03-RESEARCH.md (storage strategy)
</read_first>

<acceptance_criteria>
- node-persistence.ts service created and exports necessary functions
- Service includes load/save functions for sidecar files
- Handles missing sidecar files gracefully (as mentioned in research)
- Uses File System Access API as specified
</acceptance_criteria>

<action>
Create src/services/node-persistence.ts with:
1. loadNodePositions(filePath: string): Promise<JsonEngineState | null>
2. saveNodePositions(filePath: string, data: JsonEngineState): Promise<void>
3. function to normalize file path for sidecar filename (append .json-engine.json)
4. Handle missing sidecar gracefully by returning default/empty state
5. Proper error handling for FS API interactions
</action>
</task>

<task id="03-04-03">
<read_first>
- src/store/app-store.ts
- src/services/node-persistence.ts
- src/components/canvas/NodeCanvas.tsx
</read_first>

<acceptance_criteria>
- Store manages node positions and tracks when changed
- Position saving is debounced to 500ms as recommended in research
- Positions store as JSON path (not node ID) for durability
- Store includes functionality to check for errors before save
</acceptance_criteria>

<action>
Enhance app-store.ts to support persistence:
1. Add nodePositions: Map<string, {x: number, y: number}> (keyed by JSON path)
2. Add collapsedNodes: Map<string, boolean>
3. Add saveNodePositions: boolean (controls auto-save)
4. Add method to update position when node moved: updateNodePosition(jsonPath, position)
5. Add savePositions() method that debounces 500ms and calls persistence service
6. Modify file load to automatically restore previous positions from sidecar
7. Add canSave(): boolean method that checks for validation errors when VALD-08 required
</action>
</task>

<task id="03-04-04">
<read_first>
- src/components/canvas/NodeCanvas.tsx
- src/store/app-store.ts
- src/services/node-persistence.ts
</read_first>

<acceptance_criteria>
- NodeCanvas restores positions from persisted data on load
- Draggable positions are tracked and saved properly
- Position data survives canvas refreshes/re-renders
</acceptance_criteria>

<action>
Update NodeCanvas.tsx to support position persistence:
1. Subscribe to store.nodePositions to apply saved positions to nodes
2. Handle flowPositionChange events to update store with new positions
3. Restore view state (zoom/position) from persisted data when available
4. Ensure JSON path is properly maintained as the canonical identifier (vs node ID)
5. Test that moving nodes triggers appropriate save behavior
</action>
</task>

<task id="03-04-05">
<read_first>
- src/store/app-store.ts  
- src/components/workspace/EditorWorkspace.tsx
- .planning/REQUIREMENTS.md (VALD-08 requirement)
</read_first>

<acceptance_criteria>
- Save operation is blocked when validation errors exist
- Override mechanism allows saving despite errors (VALD-08)
- User notified why save is disabled
</acceptance_criteria>

<action>
Implement save blocking functionality to meet VALD-08 requirement:
1. Update store.canSave() to return false when validationErrors.length > 0
2. Add userOverrideSave: boolean state to allow bypassing validation
3. Implement setUserOverrideSave() method to enable override for current session
4. Update file save mechanisms to check store.canSave() before proceeding
5. Show save button disabled state when errors exist
6. Implement save override dialog/workflow in EditorWorkspace
</action>
</task>

## Verification

```bash
# Test node persistence functionality
ls src/services/node-persistence.ts
grep "nodePositions" src/store/app-store.ts
grep "JsonEngineState" src/types/index.ts
grep ".json-engine.json" src/services/node-persistence.ts
grep "canSave" src/store/app-store.ts && grep "override" src/store/app-store.ts
npm run build  # Ensure all changes compile
```

## Must Haves

1. ✅ Node positions persist to .json-engine.json sidecar file (FILE-07)
2. ✅ Node positions restore on reopen (per requirement 5)
3. ✅ Positions keyed by JSON path not node ID (as per research)
4. ✅ Saving debounced at 500ms to prevent excessive writes
5. ✅ Cannot save while errors exist (VALD-08)
6. ✅ Manual override available to force save with validation errors
7. ✅ Canvas search/filter works with persisted positions
8. ✅ Error panel functionality preserved with persisted nodes
9. ✅ Node positions persist to .json-engine.json and restore on reopen (requirement 5)
10. ✅ Cannot save while errors exist with override option (requirement 6)
11. ✅ Users can move nodes to customize layout (CANV-05)

