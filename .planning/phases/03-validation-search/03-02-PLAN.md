# Plan 03-02: Error Display

**Wave:** 2
**Depends on:** 03-01-PLAN.md (AJV Integration)
**Files modified:**
- src/components/canvas/JsonNode.tsx
- src/components/editor/CodeEditor.tsx
- src/components/panels/ErrorPanel.tsx
- src/components/workspace/EditorWorkspace.tsx
- src/types/index.ts
- src/store/app-store.ts

**Requirements:** VALD-04, VALD-05, VALD-06, VALD-07, CANV-09

## Pre-requisites
- [ ] 03-01-PLAN.md (AJV Integration) completed and tested

## Tasks

<task id="03-02-01">
<read_first>
- src/components/canvas/JsonNode.tsx
- src/types/index.ts (ValidationError interface)
- src/store/app-store.ts (validationErrors field)
</read_first>

<acceptance_criteria>
- JsonNode component displays error badge when validationErrors exist for its path
- Red badge appears as visual indicator (red dot mentioned in CANV-09)
- Error count badge shown when multiple errors on single node
</acceptance_criteria>

<action>
Modify JsonNode.tsx to:
1. Accept errors prop from parent component or subscribe to store
2. Check store.validationErrors.some(error => error.path.startsWith(nodeJsonPath))
3. Show red badge in top-right corner of node when errors exist
4. Optional: Show small error count in badge if multiple errors for node
5. Use Tailwind classes like `bg-red-500` for error badge
</action>
</task>

<task id="03-02-02">
<read_first>
- src/components/editor/CodeEditor.tsx
- src/store/app-store.ts
- node_modules/monaco-editor/ (via imports in existing editor)
</read_first>

<acceptance_criteria>
- Monaco editor shows squiggles on lines with validation errors
- Error markers appear for each validation error
- Clear markers when errors are resolved
</acceptance_criteria>

<action>
Update CodeEditor.tsx to:
1. Subscribe to store.validationErrors
2. Transform ValidationError[] to Monaco editor markers array
3. Use monaco.editor.setModelMarkers() to display erorrs in editor
4. Clear previous markers when validation runs again
5. Use severity level from validation errors to set marker importance
</action>
</task>

<task id="03-02-03">
<read_first>
- src/types/index.ts
- src/store/app-store.ts
- .planning/REQUIREMENTS.md (VALD-05 requirement)
</read_first>

<acceptance_criteria>
- ErrorPanel component created and displayed in workspace
- Lists all validation issues with explanations (VALD-06)
- Allows navigation to affected files/lines
- Error count displayed and updates in real-time
</acceptance_criteria>

<action>
Create src/components/panels/ErrorPanel.tsx to:
1. Receive validation errors from store
2. Group errors by severity and location
3. Display error message with explanation (meet VALD-06)
4. Allow clicking an error to jump to location in canvas/editor
5. Include summary stats and clear navigation
</action>
</task>

<task id="03-02-04">
<read_first>
- src/components/workspace/EditorWorkspace.tsx
- src/components/panels/ErrorPanel.tsx
- src/store/app-store.ts
</read_first>

<acceptance_criteria>
- ErrorPanel integrated into main workspace UI
- Panel visible when errors exist
- Panel height adjusts appropriately
</acceptance_criteria>

<action>
Update EditorWorkspace.tsx to:
1. Import and render ErrorPanel component
2. Conditionally show panel when store.validationErrors.length > 0
3. Add toggle to hide/show panel
4. Layout adjust to accommodate panel without crowding main content
</action>
</task>

<task id="03-02-05">
<read_first>
- src/components/canvas/JsonNode.tsx
- .planning/phases/03-validation-search/03-RESEARCH.md (hover provider pattern)
</read_first>

<acceptance_criteria>
- Hovering over nodes displays schema hints when available
- Info includes type, constraints, and descriptions from schema (VALD-07)
- Hover appears with appropriate delay and styling
</acceptance_criteria>

<action>
Enhance JsonNode.tsx to:
1. Add onMouseEnter/useTooltip to show schema information
2. Lookup schema info by node JSON path
3. Render tooltip/hover card with schema hints as defined in VALD-07
4. Show description, type, format, and other relevant schema constraints
5. Use Tailwind for styling consistent with overall design
</action>
</task>

## Verification

```bash
# Test error display components work
grep "bg-red-500" src/components/canvas/JsonNode.tsx  # Error badges
fgrep "setModelMarkers" src/components/editor/CodeEditor.tsx  # Editor squiggles
ls src/components/panels/ErrorPanel.tsx  # Panel created
grep "ValidationError" src/types/index.ts | grep -A 5 -B 5 "explanation"  # Error explanations
```

## Must Haves

1. ✅ JsonNode shows red error badge when validation errors exist for its path (CANV-09)
2. ✅ Validation errors appear as squiggles in editor (VALD-04)
3. ✅ Error panel lists all validation issues with explanations (VALD-05, VALD-06)
4. ✅ Schema hints display when hovering over nodes (VALD-07)
5. ✅ Invalid JSON shows red badges on affected nodes (requirement 1)
6. ✅ Error panel lists validation issues and explains problems (requirement 3)
7. ✅ Schema hints appear when hovering over nodes (requirement 7)

