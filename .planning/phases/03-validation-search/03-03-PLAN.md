# Plan 03-03: Canvas Search

**Wave:** 2
**Depends on:** None (Phase 2 complete, can run in parallel with 03-02)
**Files modified:**
- src/components/canvas/NodeCanvas.tsx
- src/components/canvas/SearchBar.tsx
- src/components/workspace/EditorWorkspace.tsx
- src/store/app-store.ts
- src/types/index.ts

**Requirements:** CANV-08

## Tasks

<task id="03-03-01">
<read_first>
- src/components/canvas/NodeCanvas.tsx
- src/store/app-store.ts
- .planning/phases/03-validation-search/03-RESEARCH.md (search implementation pattern)
</read_first>

<acceptance_criteria>
- SearchBar component created with input and UI controls
- Real-time filtering as user types
- Matches counted and displayed
</acceptance_criteria>

<action>
Create src/components/canvas/SearchBar.tsx with:
1. Text input field for entering search term
2. Result counter showing x of y matches
3. Clear button to reset search
4. Debounced input (150ms) to reduce performance impact
5. Keyboard support for navigation through results
</action>
</task>

<task id="03-03-02">
<read_first>
- src/types/index.ts
- src/store/app-store.ts
- src/components/canvas/NodeCanvas.tsx
</read_first>

<acceptance_criteria>
- App store tracks searchQuery and filteredNodes
- Filtering algorithm matches node labels/keys by search term
- Store updates efficiently when query changes
</acceptance_criteria>

<action>
Add search capabilities to app store in app-store.ts:
1. Add searchQuery: string field
2. Add setActiveSearchQuery(query: string) method
3. Implement filterNodes method that filters nodes based on search query
4. Update nodes state derivation to include filtered set
5. Store match count and active search context
</action>
</task>

<task id="03-03-03">
<read_first>
- src/components/canvas/NodeCanvas.tsx
- src/components/canvas/SearchBar.tsx
- src/store/app-store.ts
</read_first>

<acceptance_criteria>
- NodeCanvas renders filtered nodes differently (dimmed/invisible)
- Performance tests show sub-50ms filtering for 100 nodes
- Search UI accessible near canvas
</acceptance_criteria>

<action>
Update NodeCanvas.tsx to:
1. Integrate SearchBar component in UI
2. Use store.filteredNodes for rendering (when search is active)
3. Apply visual effect to distinguish matching vs non-matching nodes
4. Non-matching nodes get opacity: 0.2 or are visually dimmed
5. Maintain interactivity for matching nodes only
</action>
</task>

<task id="03-03-04">
<read_first>
- src/components/canvas/JsonNode.tsx
- src/types/index.ts
- src/components/canvas/NodeCanvas.tsx
</read_first>

<acceptance_criteria>
- Matching nodes highlighted in search mode
- Keyword highlighting within node content
- Visual distinction between match and non-match nodes
</acceptance_criteria>

<action>
Optionally enhance JsonNode.tsx to:
1. Highlight matching text parts within the node content
2. Apply special border/background for matching nodes
3. Change node styles when filtered (highlighting, opacity)
4. Preserve original node functionality while being filtered
</action>
</task>

<task id="03-03-05">
<read_first>
- src/components/workspace/EditorWorkspace.tsx
- src/components/canvas/SearchBar.tsx
</read_first>

<acceptance_criteria>
- Search bar accessible from main editor workspace
- Canvas search feature discoverable but non-intrusive
- Search remains active when switching between panels
</acceptance_criteria>

<action>
Integrate SearchBar with EditorWorkspace:
1. Add SearchBar to the main workspace layout
2. Consider placement - above canvas, in toolbar, or as overlay
3. Ensure search results remain stable across UI interactions
4. Maintain focus and UI consistency with rest of workspace
</action>
</task>

## Verification

```bash
# Test search functionality exists and works
ls src/components/canvas/SearchBar.tsx
grep "searchQuery" src/store/app-store.ts
grep -A 5 -B 5 "filterNodes" src/store/app-store.ts
grep -A 3 -B 3 "opacity.*0.2" src/components/canvas/
fgrep "highlight" src/components/canvas/*  # Text highlighting
```

## Must Haves

1. ✅ Search input implemented and user accessible
2. ✅ Canvas filter nodes by key name (per CANV-08 requirement)
3. ✅ Performance under 50ms for filtering (as defined in research)
4. ✅ Visual distinction between matching/non-matching nodes
5. ✅ Match counting and display functionality
6. ✅ Canvas search filters nodes by key name (requirement 4)
7. ✅ Search remains stable with editor interactions

