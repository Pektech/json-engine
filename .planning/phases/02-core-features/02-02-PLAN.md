---
phase: 02-core-features
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01-PLAN.md
files_modified:
  - src/components/canvas/JsonNode.tsx
  - src/components/canvas/NodeTypeBadge.tsx
  - src/components/canvas/JsonEdge.tsx
  - src/components/canvas/NodeCanvas.tsx
autonomous: true
requirements:
  - CANV-01
  - CANV-02
  - CANV-03
  - CANV-04
  - CANV-10
must_haves:
  truths:
    - Custom JsonNode component displays JSON values with type badges
    - Type badges show Object, Array, String, Number, Boolean, Null
    - Parent-child connections visible as styled edges
    - Nodes are selectable and highlight when selected
    - Double-click expands/collapses children
    - 70%+ test coverage for components
  artifacts:
    - path: "src/components/canvas/JsonNode.tsx"
      provides: "Custom React Flow node component"
      exports: ["JsonNode"]
    - path: "src/components/canvas/NodeTypeBadge.tsx"
      provides: "Type badge component"
      exports: ["NodeTypeBadge"]
    - path: "src/components/canvas/JsonEdge.tsx"
      provides: "Custom edge component"
      exports: ["JsonEdge"]
  key_links:
    - from: "src/components/canvas/JsonNode.tsx"
      to: "src/components/canvas/NodeCanvas.tsx"
      via: "nodeTypes prop"
      pattern: "nodeTypes.*jsonNode.*JsonNode"
    - from: "src/components/canvas/JsonNode.tsx"
      to: "src/components/canvas/NodeTypeBadge.tsx"
      via: "import and render"
      pattern: "import.*NodeTypeBadge"
---

<objective>
Create custom JsonNode component with type badges, custom edges, and interaction handlers.

Purpose: Make the canvas visually informative with type indicators, connection lines, and interactive behaviors.
Output: Styled node components with badges, edges, selection, and expand/collapse functionality.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/02-core-features/02-RESEARCH.md
@src/components/canvas/NodeCanvas.tsx (from 02-01)
@src/types/canvas.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create NodeTypeBadge component</name>
  <files>src/components/canvas/NodeTypeBadge.tsx</files>
  <read_first>
    - src/types/canvas.ts
    - src/app/globals.css or tailwind.config.ts (to see color tokens)
  </read_first>
  <action>
    Create src/components/canvas/NodeTypeBadge.tsx:

    1. Define NodeTypeBadgeProps:
       - type: JsonNodeType ('object' | 'array' | 'string' | 'number' | 'boolean' | 'null')
       - size?: 'sm' | 'md' (default 'md')
    
    2. Color scheme (use Tailwind classes):
       - object: bg-blue-100 text-blue-800 border-blue-300
       - array: bg-purple-100 text-purple-800 border-purple-300
       - string: bg-green-100 text-green-800 border-green-300
       - number: bg-orange-100 text-orange-800 border-orange-300
       - boolean: bg-yellow-100 text-yellow-800 border-yellow-300
       - null: bg-gray-100 text-gray-800 border-gray-300
    
    3. Display labels:
       - object: "Object"
       - array: "Array"
       - string: "String"
       - number: "Number"
       - boolean: "Boolean"
       - null: "Null"
    
    4. Style:
       - Rounded pill shape (rounded-full)
       - Small padding (px-2 py-0.5 for sm, px-2.5 py-1 for md)
       - Border (border)
       - Font: text-xs font-medium uppercase tracking-wide
    
    5. Export NodeTypeBadge as default export.
  </action>
  <acceptance_criteria>
    - File exists at src/components/canvas/NodeTypeBadge.tsx
    - Accepts type and size props
    - Each type has distinct color (blue, purple, green, orange, yellow, gray)
    - Uses Tailwind classes for styling
    - All JsonNodeType values handled
    - Exported as default
    - No TypeScript errors
  </acceptance_criteria>
  <verify>
    <automated>grep -q "export.*NodeTypeBadge" src/components/canvas/NodeTypeBadge.tsx && grep -q "type: JsonNodeType" src/components/canvas/NodeTypeBadge.tsx && grep -q "bg-blue-100" src/components/canvas/NodeTypeBadge.tsx && grep -q "bg-purple-100" src/components/canvas/NodeTypeBadge.tsx && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>NodeTypeBadge component with color-coded badges created</done>
</task>

<task type="auto">
  <name>Task 2: Create JsonNode component</name>
  <files>src/components/canvas/JsonNode.tsx</files>
  <read_first>
    - src/types/canvas.ts
    - src/components/canvas/NodeTypeBadge.tsx
    - .planning/phases/02-core-features/02-RESEARCH.md (see React Flow v12 NodeProps)
  </read_first>
  <action>
    Create src/components/canvas/JsonNode.tsx:

    1. Import NodeProps from '@xyflow/react'
    
    2. Define JsonNodeProps:
       - Extend NodeProps<JsonNodeData>
    
    3. Component implementation:
       - Extract data from props: { type, label, value, depth, path }
       - Use selected state from props for highlighting
       - Style container:
         - Width: 180px
         - Background: white (dark: bg-gray-800)
         - Border: 2px solid, gray-300 (dark: gray-600)
         - Border color changes to blue-500 when selected
         - Border radius: rounded-lg
         - Shadow: shadow-md
         - Padding: p-3
       - Content layout:
         - Top row: label text (font-semibold truncate)
         - Middle row: NodeTypeBadge
         - Bottom row: value preview (if primitive, truncate, max-w-full)
       - Handle click:
         - Call props.onClick if provided
       - Handle double-click:
         - Toggle expanded state (local useState)
         - Note: Actual expand/collapse logic in Wave 4 (sync)
       - Add selection styling:
         - ring-2 ring-blue-500 ring-offset-2 when selected
    
    4. Export JsonNode as default export.
  </action>
  <acceptance_criteria>
    - File exists at src/components/canvas/JsonNode.tsx
    - Imports NodeProps from '@xyflow/react'
    - Uses NodeTypeBadge component
    - Displays label, type badge, and value preview
    - Changes border color when selected (blue-500)
    - Has selection ring styling
    - Fixed width of 180px
    - Exported as default
    - No TypeScript errors
  </acceptance_criteria>
  <verify>
    <automated>grep -q "from '@xyflow/react'" src/components/canvas/JsonNode.tsx && grep -q "NodeProps" src/components/canvas/JsonNode.tsx && grep -q "NodeTypeBadge" src/components/canvas/JsonNode.tsx && grep -q "selected" src/components/canvas/JsonNode.tsx && grep -q "180px\|w-44\|w-[180px]" src/components/canvas/JsonNode.tsx && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>JsonNode component with badges and selection styling created</done>
</task>

<task type="auto">
  <name>Task 3: Create custom JsonEdge component</name>
  <files>src/components/canvas/JsonEdge.tsx</files>
  <read_first>
    - .planning/phases/02-core-features/02-RESEARCH.md (see React Flow edge types)
    - src/types/canvas.ts
  </read_first>
  <action>
    Create src/components/canvas/JsonEdge.tsx:

    1. Import BaseEdge, EdgeLabelRenderer, getSmoothStepPath from '@xyflow/react'
    
    2. Define JsonEdgeProps:
       - Extend EdgeProps from '@xyflow/react'
    
    3. Component implementation:
       - Use getSmoothStepPath for curved connection lines
       - Calculate edge path:
         - sourceX, sourceY, sourcePosition
         - targetX, targetY, targetPosition
         - borderRadius: 8
       - Style the edge:
         - Stroke: stroke-gray-400 (dark: stroke-gray-500)
         - Stroke width: 2
         - Animated: false (can add later)
       - Add edge label (optional):
         - For array items, show index
         - Position at center of path
    
    4. Alternative: Use React Flow's default SmoothStepEdge if custom not needed.
       - If creating custom, use BaseEdge component
    
    5. Export JsonEdge as default export.
  </action>
  <acceptance_criteria>
    - File exists at src/components/canvas/JsonEdge.tsx
    - Imports from '@xyflow/react'
    - Uses getSmoothStepPath or similar
    - Stroke color is gray-400
    - Stroke width is 2
    - Exported as default
    - No TypeScript errors
  </acceptance_criteria>
  <verify>
    <automated>grep -q "from '@xyflow/react'" src/components/canvas/JsonEdge.tsx && grep -q "getSmoothStepPath\|BaseEdge" src/components/canvas/JsonEdge.tsx && grep -q "export.*JsonEdge" src/components/canvas/JsonEdge.tsx && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>JsonEdge component for parent-child connections created</done>
</task>

<task type="auto">
  <name>Task 4: Update NodeCanvas with custom node and edge types</name>
  <files>src/components/canvas/NodeCanvas.tsx</files>
  <read_first>
    - src/components/canvas/NodeCanvas.tsx (current state from 02-01)
    - src/components/canvas/JsonNode.tsx
    - src/components/canvas/JsonEdge.tsx
  </read_first>
  <action>
    Update src/components/canvas/NodeCanvas.tsx:

    1. Import JsonNode component
    2. Import JsonEdge component
    
    3. Define nodeTypes object:
       - jsonNode: JsonNode
    
    4. Define edgeTypes object:
       - smoothstep: JsonEdge (or use 'default')
    
    5. Update ReactFlow props:
       - Add nodeTypes={nodeTypes}
       - Add edgeTypes={edgeTypes}
       - Add onNodeClick handler that calls props.onNodeSelect
       - Pass selected prop to nodes based on selectedNodeId
    
    6. Ensure nodes array includes selected property:
       - Map over nodes and add selected: node.id === selectedNodeId
    
    7. Add expand/collapse handler (prepare for Wave 4):
       - Create onNodeDoubleClick handler
       - For now, just log the event
    
    8. Verify dark mode support:
       - ReactFlow should use dark theme when app is dark
       - Check if ReactFlow has theme prop or uses CSS variables
  </action>
  <acceptance_criteria>
    - NodeCanvas imports JsonNode and JsonEdge
    - nodeTypes prop passed to ReactFlow with jsonNode mapping
    - edgeTypes prop passed to ReactFlow
    - onNodeClick handler calls onNodeSelect prop
    - Nodes have selected property based on selectedNodeId
    - No TypeScript errors
    - Component still renders correctly
  </acceptance_criteria>
  <verify>
    <automated>grep -q "import.*JsonNode" src/components/canvas/NodeCanvas.tsx && grep -q "import.*JsonEdge" src/components/canvas/NodeCanvas.tsx && grep -q "nodeTypes" src/components/canvas/NodeCanvas.tsx && grep -q "onNodeClick" src/components/canvas/NodeCanvas.tsx && grep -q "onNodeSelect" src/components/canvas/NodeCanvas.tsx && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>NodeCanvas updated with custom node/edge types and selection handling</done>
</task>

</tasks>

<verification>
1. NodeTypeBadge renders all 6 JSON types with correct colors
2. JsonNode displays label, badge, and value preview
3. JsonNode shows selection state (blue border + ring)
4. JsonEdge connects parent to child nodes
5. NodeCanvas maps jsonNode type to JsonNode component
6. onNodeSelect callback fires when node clicked
</verification>

<success_criteria>
- JsonNode component displays type badges (Object, Array, String, etc.)
- Nodes are visually distinct with proper colors
- Selected node has blue highlight border
- Parent-child connections visible as lines
- Double-click handler prepared (logging only in this plan)
- Components are testable with 70%+ coverage target
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-features/02-02-SUMMARY.md`
</output>
