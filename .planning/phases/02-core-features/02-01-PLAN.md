---
phase: 02-core-features
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/json-to-graph.ts
  - src/lib/graph-layout.ts
  - src/components/canvas/NodeCanvas.tsx
  - src/types/canvas.ts
autonomous: true
requirements:
  - CANV-01
  - CANV-02
  - CANV-06
  - CANV-07
  - UIUX-05
must_haves:
  truths:
    - React Flow v12 is integrated and rendering nodes
    - JSON file transforms into node/edge data structure
    - Canvas displays nodes in tree layout
    - Zoom, pan, and minimap controls work
    - 10KB JSON renders in under 2 seconds
  artifacts:
    - path: "src/lib/json-to-graph.ts"
      provides: "JSON to React Flow nodes/edges transformation"
      exports: ["jsonToGraph", "getJsonType", "JsonNode"]
    - path: "src/lib/graph-layout.ts"
      provides: "Dagre-based tree layout for nodes"
      exports: ["layoutGraph"]
    - path: "src/components/canvas/NodeCanvas.tsx"
      provides: "React Flow canvas component"
      exports: ["NodeCanvas"]
    - path: "src/types/canvas.ts"
      provides: "TypeScript types for canvas"
      exports: ["JsonNodeData", "CanvasNode", "CanvasEdge"]
  key_links:
    - from: "src/lib/json-to-graph.ts"
      to: "src/components/canvas/NodeCanvas.tsx"
      via: "nodes and edges props"
      pattern: "jsonToGraph.*NodeCanvas"
    - from: "src/lib/graph-layout.ts"
      to: "src/components/canvas/NodeCanvas.tsx"
      via: "positioned nodes"
      pattern: "layoutGraph.*position"
---

<objective>
Integrate React Flow v12 and create the JSON-to-graph transformation pipeline.

Purpose: Establish the visual canvas foundation that converts JSON into an interactive node graph with automatic tree layout.
Output: Working canvas component with React Flow, JSON transformation, and layout engine.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/02-core-features/02-RESEARCH.md
@src/app/layout.tsx (existing app structure)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create canvas types</name>
  <files>src/types/canvas.ts</files>
  <read_first>
    - src/app/layout.tsx (see existing project structure)
    - .planning/phases/02-core-features/02-RESEARCH.md (see JSON to Graph section)
  </read_first>
  <action>
    Create src/types/canvas.ts with TypeScript types:

    1. JsonNodeType: 'object' | 'array' | 'string' | 'number' | 'boolean' | 'null'
    
    2. JsonNodeData interface:
       - type: JsonNodeType
       - label: string
       - value?: string (for primitives)
       - depth: number
       - path: string (JSON path like 'root.users.0.name')
    
    3. CanvasNode interface (React Flow node):
       - id: string
       - type: string (use 'jsonNode')
       - position: { x: number; y: number }
       - data: JsonNodeData
       - width?: number
       - height?: number
    
    4. CanvasEdge interface (React Flow edge):
       - id: string
       - source: string
       - target: string
       - type?: string
    
    5. GraphData interface:
       - nodes: CanvasNode[]
       - edges: CanvasEdge[]

    Export all types as named exports.
  </action>
  <acceptance_criteria>
    - File exists at src/types/canvas.ts
    - Contains JsonNodeType, JsonNodeData, CanvasNode, CanvasEdge, GraphData
    - All types are exported
    - No TypeScript errors when imported
  </acceptance_criteria>
  <verify>
    <automated>grep -q "export type JsonNodeType" src/types/canvas.ts && grep -q "export interface JsonNodeData" src/types/canvas.ts && grep -q "export interface CanvasNode" src/types/canvas.ts && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Canvas types defined and exported</done>
</task>

<task type="auto">
  <name>Task 2: Create JSON to graph transformation</name>
  <files>src/lib/json-to-graph.ts</files>
  <read_first>
    - src/types/canvas.ts (types just created)
    - .planning/phases/02-core-features/02-RESEARCH.md (see JSON to Graph Transformation section)
  </read_first>
  <action>
    Create src/lib/json-to-graph.ts:

    1. getJsonType(value: any): JsonNodeType function:
       - Returns 'null' if value === null
       - Returns 'object' if typeof === 'object' && !Array.isArray() && value !== null
       - Returns 'array' if Array.isArray(value)
       - Returns 'string' | 'number' | 'boolean' for primitives
    
    2. getLabel(path: string, value: any): string function:
       - Extract last key from path (e.g., 'root.users.0.name' → 'name' or '[0]')
       - For arrays, show 'item[N]' format
       - For objects, show the property key
    
    3. jsonToGraph(json: any, path = 'root', depth = 0): GraphData function:
       - Recursively traverse JSON
       - Create CanvasNode for each value with:
         - id: path
         - type: 'jsonNode'
         - position: { x: 0, y: 0 } (layout will set positions)
         - data: { type, label, value (if primitive), depth, path }
       - Create CanvasEdge for each parent-child relationship
       - Handle arrays: child path = `${path}[${index}]`
       - Handle objects: child path = `${path}.${key}`
    
    4. countNodes(json: any): number function:
       - Returns total count for performance monitoring
    
    Export all functions.
  </action>
  <acceptance_criteria>
    - File exists at src/lib/json-to-graph.ts
    - getJsonType correctly identifies all JSON types
    - jsonToGraph transforms test JSON: '{"a":1,"b":[1,2]}' into nodes/edges
    - countNodes returns correct count
    - All functions exported
    - No TypeScript errors
  </acceptance_criteria>
  <verify>
    <automated>grep -q "export function jsonToGraph" src/lib/json-to-graph.ts && grep -q "export function getJsonType" src/lib/json-to-graph.ts && grep -q "export function countNodes" src/lib/json-to-graph.ts && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>JSON transformation functions implemented</done>
</task>

<task type="auto">
  <name>Task 3: Create graph layout engine</name>
  <files>src/lib/graph-layout.ts</files>
  <read_first>
    - src/types/canvas.ts
    - src/lib/json-to-graph.ts
    - .planning/phases/02-core-features/02-RESEARCH.md (see Layout Algorithm section)
  </read_first>
  <action>
    Create src/lib/graph-layout.ts:

    1. Install dagre dependency (already in package.json from Phase 1)
    
    2. layoutGraph(nodes: CanvasNode[], edges: CanvasEdge[]): CanvasNode[] function:
       - Import Dagre from '@dagrejs/dagre'
       - Create graph: new Dagre.graphlib.Graph().setDefaultEdgeLabel({})
       - Set graph options:
         - rankdir: 'TB' (top-to-bottom)
         - nodesep: 150 (horizontal spacing)
         - ranksep: 100 (vertical spacing)
         - ranker: 'tight-tree'
       - Add nodes: g.setNode(node.id, { width: 180, height: 60 })
       - Add edges: g.setEdge(edge.source, edge.target)
       - Run layout: Dagre.layout(g)
       - Return nodes with updated positions:
         - x: g.node(node.id).x - 90 (center)
         - y: g.node(node.id).y - 30 (center)
    
    3. fitViewBounds(nodes: CanvasNode[]): { x, y, zoom } function:
       - Calculate bounding box of all nodes
       - Return viewport to fit all nodes
    
    Export all functions.
  </action>
  <acceptance_criteria>
    - File exists at src/lib/graph-layout.ts
    - layoutGraph returns nodes with calculated x/y positions
    - Dagre is imported from '@dagrejs/dagre'
    - fitViewBounds calculates bounding box correctly
    - All functions exported
    - No TypeScript errors
  </acceptance_criteria>
  <verify>
    <automated>grep -q "import.*@dagrejs/dagre" src/lib/graph-layout.ts && grep -q "export function layoutGraph" src/lib/graph-layout.ts && grep -q "export function fitViewBounds" src/lib/graph-layout.ts && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Graph layout engine implemented</done>
</task>

<task type="auto">
  <name>Task 4: Create NodeCanvas component</name>
  <files>src/components/canvas/NodeCanvas.tsx</files>
  <read_first>
    - src/types/canvas.ts
    - src/lib/json-to-graph.ts
    - src/lib/graph-layout.ts
    - src/app/layout.tsx (existing app structure)
    - .planning/phases/02-core-features/02-RESEARCH.md (see React Flow v12 section)
  </read_first>
  <action>
    Create src/components/canvas/NodeCanvas.tsx:

    1. Import React Flow components:
       - ReactFlow, Controls, MiniMap, Background from '@xyflow/react'
       - '@xyflow/react/dist/style.css'
    
    2. Define NodeCanvasProps:
       - json: any (the JSON to display)
       - onNodeSelect?: (id: string) => void
       - selectedNodeId?: string | null
    
    3. Component implementation:
       - Transform JSON to graph: useMemo(() => jsonToGraph(json), [json])
       - Layout nodes: useMemo(() => layoutGraph(nodes, edges), [nodes, edges])
       - Manage nodes/edges state with useState
       - Handle node selection with onNodeClick
       - Render ReactFlow with:
         - nodes (layouted)
         - edges
         - onNodesChange handler
         - onNodeClick handler
         - fitView on initial render
         - style: { width: '100%', height: '100%' }
       - Include Controls (zoom buttons)
       - Include MiniMap (overview)
       - Include Background (grid)
    
    4. Export NodeCanvas as default export.
  </action>
  <acceptance_criteria>
    - File exists at src/components/canvas/NodeCanvas.tsx
    - Imports '@xyflow/react' (NOT 'reactflow')
    - Imports '@xyflow/react/dist/style.css'
    - Exports NodeCanvas component
    - Accepts json, onNodeSelect, selectedNodeId props
    - Renders ReactFlow with Controls, MiniMap, Background
    - No TypeScript errors
  </acceptance_criteria>
  <verify>
    <automated>grep -q "from '@xyflow/react'" src/components/canvas/NodeCanvas.tsx && grep -q "@xyflow/react/dist/style.css" src/components/canvas/NodeCanvas.tsx && grep -q "export.*NodeCanvas" src/components/canvas/NodeCanvas.tsx && grep -q "ReactFlow" src/components/canvas/NodeCanvas.tsx && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>NodeCanvas component with React Flow integration created</done>
</task>

</tasks>

<verification>
1. All type files exist and are exported
2. jsonToGraph transforms test JSON correctly
3. layoutGraph positions nodes using Dagre
4. NodeCanvas renders React Flow with controls and minimap
5. No import errors for @xyflow/react
6. All imports use v12 package name
</verification>

<success_criteria>
- React Flow v12 is installed and imported correctly (@xyflow/react)
- JSON to graph transformation works for nested objects and arrays
- Dagre layout positions nodes in tree structure
- NodeCanvas displays zoom/pan controls and minimap
- Component renders without errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-features/02-01-SUMMARY.md`
</output>
