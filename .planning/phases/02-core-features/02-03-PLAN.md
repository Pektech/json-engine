---
phase: 02-core-features
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01-PLAN.md
files_modified:
  - src/components/editor/CodeEditor.tsx
  - src/components/editor/EditorToolbar.tsx
  - src/lib/path-to-line.ts
autonomous: true
requirements:
  - EDIT-01
  - EDIT-02
  - EDIT-03
  - EDIT-04
  - EDIT-06
must_haves:
  truths:
    - Monaco Editor displays JSON with syntax highlighting
    - JSON validation shows syntax errors inline
    - Path breadcrumbs show current node location
    - Format Document command available
    - Editor scrolls to line when node selected
    - 70%+ test coverage for editor components
  artifacts:
    - path: "src/components/editor/CodeEditor.tsx"
      provides: "Monaco Editor wrapper component"
      exports: ["CodeEditor"]
    - path: "src/components/editor/EditorToolbar.tsx"
      provides: "Editor toolbar with actions"
      exports: ["EditorToolbar"]
    - path: "src/lib/path-to-line.ts"
      provides: "JSON path to line number mapping"
      exports: ["pathToLine", "lineToPath"]
  key_links:
    - from: "src/components/editor/CodeEditor.tsx"
      to: "src/lib/path-to-line.ts"
      via: "scrollToLine function"
      pattern: "import.*path-to-line"
    - from: "src/components/editor/CodeEditor.tsx"
      to: "@monaco-editor/react"
      via: "Editor component"
      pattern: "import.*@monaco-editor/react"
---

<objective>
Integrate Monaco Editor with JSON language mode and editor features.

Purpose: Provide a professional code editing experience with syntax highlighting, validation, breadcrumbs, and formatting.
Output: Monaco Editor component with JSON support, validation, breadcrumbs, and scroll-to-line capability.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/02-core-features/02-RESEARCH.md
@src/app/layout.tsx (existing app structure)
@src/components/canvas/NodeCanvas.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create path-to-line mapping utility</name>
  <files>src/lib/path-to-line.ts</files>
  <read_first>
    - .planning/phases/02-core-features/02-RESEARCH.md (see Bidirectional Sync section)
  </read_first>
  <action>
    Create src/lib/path-to-line.ts:

    1. parseJsonWithLocation(jsonText: string): { data: any; locations: Map<string, { line: number; column: number }> }
       - Parse JSON while tracking line numbers for each key/value
       - Return parsed data and a map of paths to line/column
       - Path format: 'root.users[0].name'
    
    2. pathToLine(jsonText: string, path: string): { line: number; column: number } | null
       - Parse JSON and find line number for given path
       - Return null if path not found
       - Use parseJsonWithLocation internally
    
    3. lineToPath(jsonText: string, line: number): string | null
       - Find JSON path at given line number
       - Walk through JSON and find deepest path for the line
       - Return path or null if not found
    
    4. Helper: getLinePosition(text: string, index: number): { line: number; column: number }
       - Count newlines before index to get line number
    
    5. Export all functions.
    
    Note: For initial implementation, can use a simple regex-based approach:
    - Split JSON by newlines
    - Find line containing the path key
    - Track depth based on brackets/braces
  </action>
  <acceptance_criteria>
    - File exists at src/lib/path-to-line.ts
    - pathToLine returns line number for 'root.users[0].name'
    - lineToPath returns path for given line number
    - Handles nested objects and arrays
    - Returns null for invalid paths
    - All functions exported
    - No TypeScript errors
    - Unit tests pass
  </acceptance_criteria>
  <verify>
    <automated>grep -q "export function pathToLine" src/lib/path-to-line.ts && grep -q "export function lineToPath" src/lib/path-to-line.ts && grep -q "export function parseJsonWithLocation" src/lib/path-to-line.ts && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Path-to-line mapping utility created</done>
</task>

<task type="auto">
  <name>Task 2: Create CodeEditor component</name>
  <files>src/components/editor/CodeEditor.tsx</files>
  <read_first>
    - src/lib/path-to-line.ts
    - .planning/phases/02-core-features/02-RESEARCH.md (see Monaco Editor section)
    - src/app/globals.css or tailwind.config.ts
  </read_first>
  <action>
    Create src/components/editor/CodeEditor.tsx:

    1. Import Editor from '@monaco-editor/react'
    2. Import { monaco } from '@monaco-editor/react' for beforeMount
    3. Import pathToLine from '@/lib/path-to-line'
    
    4. Define CodeEditorProps:
       - value: string (JSON text)
       - onChange?: (value: string) => void
       - onValidate?: (markers: any[]) => void
       - selectedPath?: string | null
       - theme?: 'vs-dark' | 'light'
    
    5. Component state:
       - editorRef: useRef for editor instance
    
    6. Monaco initialization (beforeMount):
       - Set JSON diagnostics options (validate: true)
       - Configure JSON schema (basic schema for now, Phase 3 adds OpenClaw schema)
    
    7. Editor mount (onMount):
       - Save editor instance to ref
       - Set up command for Format Document (Ctrl+Shift+I)
       - Add custom action for breadcrumbs
    
    8. Handle value changes (onChange):
       - Debounce with 100ms
       - Call props.onChange with new value
    
    9. Handle validation (onValidate):
       - Call props.onValidate with markers
    
    10. Scroll to line effect (useEffect):
        - Watch selectedPath prop
        - If changed and not null:
          - Call pathToLine(value, selectedPath) to get line
          - If line found: editorRef.current.revealLineInCenter(line)
          - Set cursor position: editorRef.current.setPosition({ lineNumber: line, column: 1 })
    
    11. Format function:
        - Expose via ref or prop: formatDocument()
        - Call editorRef.current.getAction('editor.action.formatDocument').run()
    
    12. Render Editor with:
        - height: "100%"
        - language: "json"
        - theme: props.theme || 'vs-dark'
        - value: props.value
        - options:
          - minimap: { enabled: false }
          - scrollBeyondLastLine: false
          - automaticLayout: true
          - tabSize: 2
          - formatOnPaste: true
          - formatOnType: true
          - renderWhitespace: 'selection'
        - onChange: handleChange
        - onMount: handleMount
        - beforeMount: handleBeforeMount
    
    13. Export CodeEditor as default export with forwardRef for imperative handle.
  </action>
  <acceptance_criteria>
    - File exists at src/components/editor/CodeEditor.tsx
    - Imports Editor from '@monaco-editor/react'
    - Accepts value, onChange, onValidate, selectedPath props
    - Renders Monaco Editor with JSON language mode
    - Scrolling to line when selectedPath changes (via pathToLine)
    - Format action available
    - Dark theme by default
    - No TypeScript errors
    - Component exported
  </acceptance_criteria>
  <verify>
    <automated>grep -q "from '@monaco-editor/react'" src/components/editor/CodeEditor.tsx && grep -q "language=\"json\"" src/components/editor/CodeEditor.tsx && grep -q "pathToLine" src/components/editor/CodeEditor.tsx && grep -q "revealLineInCenter" src/components/editor/CodeEditor.tsx && grep -q "export.*CodeEditor" src/components/editor/CodeEditor.tsx && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>CodeEditor component with Monaco and scroll-to-line created</done>
</task>

<task type="auto">
  <name>Task 3: Create EditorToolbar component</name>
  <files>src/components/editor/EditorToolbar.tsx</files>
  <read_first>
    - src/components/editor/CodeEditor.tsx
    - src/app/globals.css or existing UI components
  </read_first>
  <action>
    Create src/components/editor/EditorToolbar.tsx:

    1. Define EditorToolbarProps:
       - onFormat?: () => void
       - onSearch?: () => void
       - errorCount?: number
       - warningCount?: number
    
    2. Component implementation:
       - Container: flex row, justify-between, border-bottom
       - Left section:
         - Format button with icon (AlignLeft from lucide-react or similar)
         - Label: "Format Document"
         - Tooltip: "Ctrl+Shift+I"
       - Middle section:
         - Breadcrumbs showing current path (from selectedPath prop)
         - Format: root > users > [0] > name
       - Right section:
         - Error indicator: red dot + count
         - Warning indicator: yellow dot + count
         - Search button with icon (Search icon)
    
    3. Styling:
       - Height: h-10 or h-12
       - Background: matches app theme
       - Border: border-b border-gray-200 (dark: border-gray-700)
       - Button styles: rounded, hover:bg-gray-100 (dark: hover:bg-gray-800)
    
    4. Accessibility:
       - aria-label on buttons
       - Keyboard shortcuts hints
    
    5. Export EditorToolbar as default export.
  </action>
  <acceptance_criteria>
    - File exists at src/components/editor/EditorToolbar.tsx
    - Contains Format button
    - Contains Search button
    - Shows error/warning counts
    - Shows breadcrumbs path
    - Uses consistent styling with app
    - Exported as default
    - No TypeScript errors
  </acceptance_criteria>
  <verify>
    <automated>grep -q "export.*EditorToolbar" src/components/editor/EditorToolbar.tsx && grep -q "onFormat" src/components/editor/EditorToolbar.tsx && grep -q "errorCount" src/components/editor/EditorToolbar.tsx && grep -q "breadcrumbs\|Breadcrumb" src/components/editor/EditorToolbar.tsx && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>EditorToolbar with format, search, and breadcrumbs created</done>
</task>

<task type="auto">
  <name>Task 4: Add breadcrumb state management</name>
  <files>src/components/editor/CodeEditor.tsx</files>
  <read_first>
    - src/components/editor/CodeEditor.tsx (current implementation)
    - src/components/editor/EditorToolbar.tsx
    - src/lib/path-to-line.ts
  </read_first>
  <action>
    Update src/components/editor/CodeEditor.tsx:

    1. Add cursor tracking:
       - Add onCursorPositionChange prop?: (path: string | null) => void
       - Listen to editor.onDidChangeCursorPosition
       - Get current line number from position
       - Call lineToPath(value, lineNumber) to get path
       - Call onCursorPositionChange with path
    
    2. Add imperative handle for external access:
       - Use forwardRef
       - Expose: { format: () => void; getPathAtLine: (line: number) => string | null }
    
    3. Update component signature:
       - Add forwardRef wrapper
       - Return imperative handle object
    
    4. Ensure compatibility with EditorToolbar:
       - CodeEditor should export current path for breadcrumbs
       - EditorToolbar receives path as prop
  </action>
  <acceptance_criteria>
    - CodeEditor uses forwardRef
    - Exposes format() method via ref
    - Exposes getPathAtLine() method via ref
    - Tracks cursor position changes
    - Calls onCursorPositionChange with JSON path
    - No breaking changes to existing props
    - No TypeScript errors
  </acceptance_criteria>
  <verify>
    <automated>grep -q "forwardRef" src/components/editor/CodeEditor.tsx && grep -q "useImperativeHandle" src/components/editor/CodeEditor.tsx && grep -q "onDidChangeCursorPosition" src/components/editor/CodeEditor.tsx && grep -q "getPathAtLine" src/components/editor/CodeEditor.tsx && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>CodeEditor updated with cursor tracking and imperative handle</done>
</task>

</tasks>

<verification>
1. Monaco Editor renders with JSON syntax highlighting
2. pathToLine converts 'root.users[0].name' to line number
3. lineToPath converts line number to JSON path
4. CodeEditor scrolls to line when selectedPath changes
5. Format Document command works
6. EditorToolbar displays breadcrumbs, errors, format button
7. CodeEditor exposes imperative handle with format() method
8. Cursor position changes trigger onCursorPositionChange
</verification>

<success_criteria>
- Monaco Editor displays JSON with syntax highlighting
- JSON validation shows errors inline (squiggles)
- Path breadcrumbs show current node location
- Format Document command works (Ctrl+Shift+I)
- Editor scrolls to line when node selected in canvas
- Editor tracks cursor position and reports path
- EditorToolbar shows error/warning counts
- Components are testable with 70%+ coverage target
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-features/02-03-SUMMARY.md`
</output>
