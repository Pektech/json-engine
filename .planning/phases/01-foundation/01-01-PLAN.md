# Plan 01-01: Initialize Next.js Project

## Goal
Initialize Next.js 14 project with TypeScript, Tailwind CSS, and complete folder structure for JSON.engine.

## Context
Starting from an empty directory (except for existing SPEC.md and design files). Need to set up the complete project foundation with proper TypeScript configuration, Tailwind CSS with custom design tokens, and folder structure as specified in the architecture.

## Must Do
- [ ] Initialize Next.js 14 with TypeScript (App Router)
- [ ] Configure Tailwind CSS with design tokens from DESIGN.md
- [ ] Create complete folder structure (src/app, src/components, src/stores, src/lib, src/hooks, src/types)
- [ ] Install all required dependencies (React Flow, Monaco, Zustand, AJV)
- [ ] Configure tsconfig.json with strict mode
- [ ] Set up dark mode by default
- [ ] Create root layout.tsx with proper structure
- [ ] Create initial page.tsx placeholder
- [ ] Add .env.example file
- [ ] Verify `npm run dev` starts successfully

## Must Not Do
- Do NOT create any components yet (that's Plan 01-02)
- Do NOT implement file operations yet (that's Plan 01-03)
- Do NOT add any business logic

## Technical Details

### Design Tokens (from DESIGN.md)
```javascript
// tailwind.config.ts
colors: {
  surface: '#131313',
  'surface-container-lowest': '#0e0e0e',
  'surface-container-high': '#2a2a2a',
  primary: '#9fcaff',
  'primary-container': '#007acc',
  'outline-variant': '#404751',
  error: '#ffb4ab',
  tertiary: '#ffb784',
  // ... see DESIGN.md for complete list
}
```

### Dependencies to Install
```json
{
  "dependencies": {
    "next": "^14.0.0",
    "react": "^18.0.0",
    "react-dom": "^18.0.0",
    "reactflow": "^11.x",
    "@monaco-editor/react": "^4.x",
    "zustand": "^4.x",
    "ajv": "^8.x"
  },
  "devDependencies": {
    "typescript": "^5.x",
    "@types/node": "^20.x",
    "@types/react": "^18.x",
    "tailwindcss": "^3.x",
    "postcss": "^8.x",
    "autoprefixer": "^10.x"
  }
}
```

### Folder Structure
```
src/
├── app/
│   ├── layout.tsx
│   ├── page.tsx
│   └── globals.css
├── components/
│   ├── layout/
│   ├── canvas/
│   ├── editor/
│   └── panels/
├── stores/
├── lib/
├── hooks/
└── types/
```

## Verification Steps
1. Run `npm install` completes without errors
2. Run `npm run dev` starts development server on localhost:3000
3. No TypeScript errors in console
4. Tailwind CSS is active (test with a Tailwind class)
5. Dark theme renders correctly

## Expected Output
- package.json with all dependencies
- tsconfig.json configured
- tailwind.config.ts with design tokens
- src/ folder structure created
- app/layout.tsx and page.tsx functional
- .env.example created

## Estimated Effort
1-2 hours
