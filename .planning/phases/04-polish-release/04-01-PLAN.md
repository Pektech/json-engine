---
phase: 04-polish-release
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - next.config.js
  - package.json
  - src/components/editor/CodeEditor.tsx
  - src/components/canvas/NodeCanvas.tsx
  - src/app/layout.tsx
  - src/app/page.tsx
autonomous: true
requirements:
  - UIUX-04
  - EDIT-05
must_haves:
  truths:
    - Initial bundle size is under 300KB
    - Monaco Editor loads in background after initial render
    - Canvas (React Flow) is code-split from main bundle
    - Dev server runs on port 3030
    - Build fails if bundle exceeds 300KB budget
  artifacts:
    - path: next.config.js
      provides: Bundle analyzer configuration and bundle budget enforcement
      min_lines: 30
    - path: package.json
      provides: @next/bundle-analyzer dependency and dev script with port 3030
      contains: "@next/bundle-analyzer"
    - path: src/components/editor/CodeEditor.tsx
      provides: Lazy-loaded Monaco Editor with preload
      exports: ["CodeEditor"]
    - path: src/components/canvas/NodeCanvas.tsx
      provides: Code-split React Flow canvas
      exports: ["NodeCanvas"]
  key_links:
    - from: src/app/page.tsx
      to: src/components/editor/CodeEditor.tsx
      via: dynamic import with suspense
      pattern: "dynamic\\(.*?CodeEditor"
    - from: src/app/page.tsx
      to: src/components/canvas/NodeCanvas.tsx
      via: dynamic import with suspense
      pattern: "dynamic\\(.*?NodeCanvas"
    - from: next.config.js
      to: bundle analyzer
      via: webpack plugin configuration
      pattern: "BundleAnalyzerPlugin|bundleAnalyzer"
user_setup: []
---

<objective>
Implement performance optimization through code splitting, lazy loading, and bundle size monitoring. Configure dev server on port 3030.

Purpose: Meet the <300KB initial bundle requirement and improve perceived load time through staged loading.
Output: Code-split application with bundle analyzer and budget enforcement.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-polish-release/04-CONTEXT.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/STACK.md

## Key Decisions from CONTEXT.md
- D-01: Preload Monaco Editor in background after initial render (not on-demand)
- D-02: Show app shell immediately, Monaco loads while user can already see UI
- D-03: Split canvas (React Flow) from editor bundle — parallel loading with Monaco
- D-04: Three-stage loading: Core app (~150KB) → Canvas (~100KB) → Monaco (~1MB preloaded)
- D-05: This achieves the <300KB initial bundle target
- D-06: Use @next/bundle-analyzer for automated bundle size tracking
- D-07: Build should fail if initial bundle exceeds 300KB budget
- D-08: Generate reports during build for ongoing monitoring
- D-41: Default dev server port: 3030 (avoid 3000 conflicts)
- D-42: Configure in package.json scripts and next.config.js

## Implementation Notes
- Monaco likely currently loaded synchronously - needs dynamic import wrapper
- React Flow canvas may be bundled with app shell - needs code split boundary
- Current next.config.js is minimal - needs bundle analyzer config
- No current code splitting in place
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Install @next/bundle-analyzer and configure build budget</name>
  <files>package.json, next.config.js</files>
  <read_first>
    - package.json (check existing scripts and dependencies)
    - next.config.js (current configuration)
  </read_first>
  <action>
    1. Install @next/bundle-analyzer:
       npm install --save-dev @next/bundle-analyzer
    
    2. Update next.config.js to include bundle analyzer with budget enforcement:
       - Import withBundleAnalyzer from @next/bundle-analyzer
       - Wrap the NextConfig export with withBundleAnalyzer()
       - Add bundle analyzer config: analyzerMode: 'static', openAnalyzer: false
       - Add webpack configuration to enforce 300KB budget on initial JS chunks
       - Configure dev server port: 3030 in devServer config
       - Set poweredByHeader: false for security
       - Add compress: true for gzip compression
    
    3. Update package.json scripts:
       - Change "dev": "next dev" to "dev": "next dev -p 3030"
       - Add "analyze": "cross-env ANALYZE=true next build" script
       - Add "build:analyze": "npm run analyze" for convenience
    
    Per D-06, D-07, D-08, D-41, D-42.
  </action>
  <verify>
    <automated>
      - grep -q "@next/bundle-analyzer" package.json
      - grep -q "next dev -p 3030" package.json
      - grep -q "withBundleAnalyzer" next.config.js
      - grep -q "analyzerMode" next.config.js
      - grep -q "3030" next.config.js || grep -q "port.*3030" next.config.js
    </automated>
  </verify>
  <done>Bundle analyzer installed, next.config.js configured with budget enforcement and port 3030</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create lazy-loaded Monaco Editor wrapper</name>
  <files>src/components/editor/CodeEditor.tsx, src/components/editor/CodeEditorLoader.tsx</files>
  <read_first>
    - src/components/editor/CodeEditor.tsx (current implementation)
    - src/app/page.tsx (how CodeEditor is currently imported)
  </read_first>
  <action>
    1. Create src/components/editor/CodeEditorLoader.tsx:
       - A lightweight wrapper component that shows loading state
       - Displays a skeleton or "Loading editor..." message
       - Use design tokens: bg-surface-container-high, text-on-surface-variant
       - Follow the no-line rule (no borders, use background shifts)
    
    2. Modify src/components/editor/CodeEditor.tsx:
       - Keep the existing component implementation
       - Ensure it exports the component as default for dynamic import compatibility
       - Add explicit security comment per D-23 about token handling if applicable
    
    3. The dynamic import will be set up in page.tsx in Task 3
    
    Per D-01, D-02. Loading state follows design system conventions.
  </action>
  <verify>
    <automated>
      - test -f src/components/editor/CodeEditorLoader.tsx
      - grep -q "Loading" src/components/editor/CodeEditorLoader.tsx
      - grep -q "bg-surface-container" src/components/editor/CodeEditorLoader.tsx || grep -q "skeleton" src/components/editor/CodeEditorLoader.tsx
    </automated>
  </verify>
  <done>CodeEditorLoader component exists with loading state styled per design tokens</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Implement code splitting with dynamic imports</name>
  <files>src/app/page.tsx, src/app/layout.tsx</files>
  <read_first>
    - src/app/page.tsx (current imports and component structure)
    - src/app/layout.tsx (root layout, may need suspense wrapper)
    - src/components/canvas/NodeCanvas.tsx (current implementation)
  </read_first>
  <action>
    1. Update src/app/page.tsx:
       - Replace static imports: import dynamic from 'next/dynamic'
       - Create dynamic import for CodeEditor:
         const CodeEditor = dynamic(() => import('../components/editor/CodeEditor'), {
           ssr: false,
           loading: () => <CodeEditorLoader />
         })
       - Create dynamic import for NodeCanvas:
         const NodeCanvas = dynamic(() => import('../components/canvas/NodeCanvas'), {
           ssr: false,
           loading: () => <NodeCanvasLoader /> // create similar loader
         })
       - Import React, { Suspense } if not already present
       - Wrap dynamic components in Suspense boundaries with appropriate fallbacks
    
    2. Create src/components/canvas/NodeCanvasLoader.tsx:
       - Similar to CodeEditorLoader - shows loading state
       - Use design tokens for consistency
    
    3. Update src/app/layout.tsx if needed:
       - Ensure it has proper suspense boundaries for the dynamic imports
       - Keep the loading minimal at root level
    
    Per D-03, D-04. The goal is staged loading: Core app (~150KB) → Canvas (~100KB) → Monaco (~1MB preloaded).
  </action>
  <verify>
    <automated>
      - grep -q "next/dynamic" src/app/page.tsx
      - grep -q "dynamic(.*CodeEditor" src/app/page.tsx
      - grep -q "dynamic(.*NodeCanvas" src/app/page.tsx
      - test -f src/components/canvas/NodeCanvasLoader.tsx
      - grep -q "ssr: false" src/app/page.tsx
    </automated>
  </verify>
  <done>Both CodeEditor and NodeCanvas are dynamically imported with loading states and SSR disabled</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Add preload hints for Monaco and canvas bundles</name>
  <files>src/app/layout.tsx</files>
  <read_first>
    - src/app/layout.tsx (current head/meta configuration)
  </read_first>
  <action>
    1. Update src/app/layout.tsx to preload critical chunks:
       - Add preload links for Monaco editor chunks in the head
       - Use next/head to inject preload hints
       - Preload Monaco after initial render using useEffect
       - Preload canvas bundle in parallel
       - The preload should happen after the core app is interactive
    
    2. Implementation approach:
       - Import { useEffect } from 'react'
       - In a client component or useEffect in layout, trigger preload:
         useEffect(() => {
           // Preload Monaco and Canvas after initial render
           const preloadEditor = () => import('../components/editor/CodeEditor')
           const preloadCanvas = () => import('../components/canvas/NodeCanvas')
           requestIdleCallback?.(() => {
             preloadEditor()
             preloadCanvas()
           }) || setTimeout(() => {
             preloadEditor()
             preloadCanvas()
           }, 1000)
         }, [])
    
    Per D-01, D-02. Monaco loads in background while user already sees UI.
  </action>
  <verify>
    <automated>
      - grep -q "preloadEditor\|preloadCanvas" src/app/layout.tsx || grep -q "useEffect" src/app/layout.tsx
      - grep -q "requestIdleCallback\|setTimeout" src/app/layout.tsx
      - grep -q "import.*CodeEditor\|import.*NodeCanvas" src/app/layout.tsx
    </automated>
  </verify>
  <done>Monaco and canvas bundles are preloaded after initial render using useEffect</done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Verify bundle size and build passes</name>
  <files>package.json, next.config.js</files>
  <read_first>
    - next.config.js (verify webpack budget configuration)
  </read_first>
  <action>
    1. Run build to verify configuration works:
       npm run build
    
    2. Check that build completes without errors
    
    3. Verify bundle analyzer generated report in .next/analyze/
    
    4. If build fails due to bundle size:
       - Review the error message
       - Adjust budget temporarily if needed for initial implementation
       - Document actual bundle sizes for future optimization
    
    5. Verify dev server starts on port 3030:
       npm run dev &
       sleep 5
       curl -s http://localhost:3030 | head -1
       kill %1 2>/dev/null || true
    
    Per D-05, D-07. Initial bundle should be <300KB after code splitting.
  </action>
  <verify>
    <automated>
      - test -d .next/analyze || echo "Bundle analyzer output may be in different location"
      - npm run build 2>&1 | tail -20
    </automated>
  </verify>
  <done>Build completes, bundle analyzer generates reports, dev server configured for port 3030</done>
</task>

</tasks>

<verification>
1. Run npm run build and verify it completes successfully
2. Check .next/analyze/ for bundle analysis reports
3. Run npm run dev and verify server starts on port 3030
4. Check initial page load - should show app shell immediately
5. Monaco and canvas should load in background (visible in network tab)
</verification>

<success_criteria>
- @next/bundle-analyzer is installed and configured
- next.config.js has bundle budget enforcement (300KB limit)
- Dev server runs on port 3030 (npm run dev)
- CodeEditor is dynamically imported with loading state
- NodeCanvas is dynamically imported with loading state
- Monaco and canvas bundles preload after initial render
- Build completes successfully
</success_criteria>

<output>
After completion, create `.planning/phases/04-polish-release/04-01-SUMMARY.md`
</output>
</content>
